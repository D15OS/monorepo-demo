{
  "name": "monorepo-demo",
  "private": true,
  "scripts": {
    "build:component": "pnpm --filter ./components/** run build",
    "build:core": "pnpm --filter ./packages/** run build"
  },
  "dependencies": {
    "vue": "^3.3.4"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.2.3",
    "typescript": "^5.1.6",
    "vite": "^4.4.9",
    "vue-tsc": "^1.8.5"
  }
}